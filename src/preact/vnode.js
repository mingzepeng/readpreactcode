/** Virtual DOM Node */
export function VNode() {
    /*  这是 生成虚拟 dom 的代码，可以看出虚拟dom 大致包含以下几个属性
    let p = new VNode();
	p.nodeName = nodeName;
	p.children = children;
	p.attributes = attributes==null ? undefined : attributes;
	p.key = attributes==null ? undefined : attributes.key;
     */

}

/**
 * 
 * class VNode {
 *  nodeName : string|Component
 *  children : any[]
 *  attributes : Object;
 *  key : string
 * }
 * 
 * 
 */
